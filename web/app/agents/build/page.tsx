import { WizardShell } from "@/components/wizard/WizardShell";

export const metadata = {
  title: "Build an agent · Make_Skills",
};

export default function BuildAgentPage() {
  return <WizardShell />;
}
