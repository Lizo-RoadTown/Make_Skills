---
title: Architecture
description: The clean lines — what's shared, what's per-user, what's per-tenant; and how that maps to Docker containers in self-host vs hosted modes.
---
This document draws the lines. Every contribution from 2026-04-28 onward respects these layers.

## Two deployment modes (always supported, parallel)

| | Self-host | Hosted-multitenant |
|---|---|---|
| **Who runs it** | The user, on their own machine or VPS | Liz, on humancensys.com |
| **Auth** | None (single user) | OAuth (provider TBD) |
| **Data isolation** | Trivial (one user) | `tenant_id` everywhere |
| **Cost model** | User pays compute + their own API keys | Bring-your-own API keys; Liz pays infra |
| **Same codebase** | Yes — `PLATFORM_MODE` env var selects mode | Same |

## The five layers

```
LAYER 1: Platform code (always shared, open-source)
  ├─ platform/api/       FastAPI, deepagents runtime, tools
  ├─ web/                Next.js UI shell
  ├─ skills/             curated skill library (read-only common)
  └─ subagents/          template subagents

LAYER 2: Tenant identity & isolation (mode-dependent)
  ├─ Self-host:          tenant_id = "default"; no auth code path runs
  └─ Hosted:             tenant_id from verified OAuth token

LAYER 3: Tenant configuration (per-tenant, user-editable)
  ├─ AGENTS.md           orchestrator persona
  ├─ deepagents.toml     model + skill paths
  ├─ Their subagents     personalized clan
  └─ Their skill choices what's loaded into their agent

LAYER 4: Tenant data (per-tenant, isolated, never shared without explicit publish)
  ├─ Conversations       (LangGraph checkpoints in postgres, scoped by thread)
  ├─ Semantic memory     (LanceDB records, tenant-scoped)
  ├─ Roadmap             (their ROADMAP.md)
  ├─ Knowledge graph     (their canonical nodes — when 3c lands)
  └─ Promotion log       (their skill→tool history)

LAYER 5: Publishable content (opt-in shared)
  ├─ Skills they author and publish
  ├─ Agents they share
  └─ Knowledge graph contributions to a public commons (when 3c lands)
```

## Container topology — the line you keep asking me to draw

**This is the question that matters for both modes.** Three real options:

### Option X — One Docker stack, multi-tenant via `tenant_id`

```
┌─ ONE Docker stack on Liz's hosted infra ──────────────┐
│                                                        │
│   FastAPI (single)        ← all tenants hit this       │
│   Postgres (single)       ← rows scoped by tenant_id   │
│   LanceDB (single dir)    ← table per tenant           │
│   Grafana (single)        ← dashboards per tenant      │
│                                                        │
└────────────────────────────────────────────────────────┘
```

**Pros:** cheap, simple, one thing to operate, what most SaaS does.
**Cons:** noisy-neighbor risk, one bug can leak data, harder to give users full control.

### Option Y — Per-user Docker stack (each user = their own deployment)

```
User A:  ┌─ Docker stack ──┐    User B:  ┌─ Docker stack ──┐
         │ FastAPI         │             │ FastAPI         │
         │ Postgres        │             │ Postgres        │
         │ LanceDB         │             │ LanceDB         │
         │ Grafana         │             │ Grafana         │
         └─────────────────┘             └─────────────────┘
         their machine OR                 their machine OR
         their Render service             their Render service
```

**Pros:** complete data isolation (their data on their infra), user owns it all, simpler legal/regulatory story, matches the "open-source self-host" pillar literally.
**Cons:** more $$ if Liz hosts (one Render service per user), harder to operate at scale, no cross-tenant features.

### Option Z — Hybrid: shared platform, per-user data containers

```
┌─ Shared Docker (Liz operates) ─────────────────────────┐
│   FastAPI gateway · auth · public docs · public skills │
└──────────────┬─────────────────────────────────────────┘
               │ routes per-tenant requests to:
               │
   ┌───────────▼──────────┐    ┌──────────────────────┐
   │ User A's data pod    │    │ User B's data pod    │
   │ Postgres + LanceDB   │    │ Postgres + LanceDB   │
   └──────────────────────┘    └──────────────────────┘
```

**Pros:** best of both — shared platform code + isolated user data.
**Cons:** most complex to operate, requires per-user containers + a routing layer.

## What we have today, what's planned, where the gaps are

| Mode | Topology today | Topology planned |
|------|----------------|------------------|
| **Self-host** | ✓ Option X with tenant_id="default" — one Docker stack on user's machine, single user. Already shipped. | Stay on this — it's the right choice. |
| **Hosted (humancensys.com)** | ⚠ Same single-stack as self-host but UI on Vercel, agent backend not yet on Render. NO multi-tenancy yet. | **Choice needed:** X (shared with tenant_id), Y (per-user instances), or Z (hybrid). |

## The right framing — engine + commons + personal toolkit

The user's actual vision (clarified 2026-04-28):

> "Some of the architecture is things that should be individual to a user, period — whether we are all using the same engine for something else, some larger organizational thing. I want to build out a larger organizational thing and have something that allows everyone to create their own personal agents and agentic toolkit."

This maps cleanly to a three-tier model that's how most modern multi-user platforms actually work (GitHub, HuggingFace, Notion, Linear):

```
┌─ ENGINE — the larger organizational thing (shared by everyone) ──────────┐
│                                                                           │
│   Platform code · auth · public docs · runtime infrastructure            │
│   The FastAPI app, the deepagents runtime, LanceDB engine, Postgres      │
│                                                                           │
└──────────────────────────┬────────────────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────────────────┐
        │                  │                               │
┌───────▼────────┐  ┌──────▼──────────┐  ┌────────────────▼───────────┐
│ COMMONS        │  │ PERSONAL        │  │ PERSONAL                    │
│ (shared by     │  │ TOOLKIT (User A)│  │ TOOLKIT (User B)            │
│  everyone)     │  │                 │  │                             │
│                │  │ Their clan      │  │ Their clan                  │
│ Public skills  │  │ Their convos    │  │ Their convos                │
│ Public agents  │  │ Their memory    │  │ Their memory                │
│ Public knowledge graph (opt-in publish)│  Their roadmap              │
│ Public templates│  │ Their KG canon  │  │ Their KG canon              │
└────────────────┘  └─────────────────┘  └─────────────────────────────┘
```

**The engine is the shared organizational thing.** Everyone runs on the same FastAPI, the same deepagents, the same LanceDB binary, the same Grafana — that's "the engine."

**The commons is the shared *content* layer.** Public skills people can browse and install; public agent templates (specialist creatures); a public knowledge commons users opt into. This is what makes the project a *platform* and not just a *runtime*.

**The personal toolkit is everything individual to a user, period.** Their conversations, their memory, their clan, their roadmap, their private knowledge canon — these never share with another user without explicit publish.

## Recommendation: Option X with explicit commons

**Single shared Docker stack** (the engine), with `tenant_id` partitioning all user-private data. The commons is just data marked `public = true` (or in a special namespace) that any tenant can read.

| Layer | Container topology | Data isolation |
|-------|--------------------|----------------|
| **Engine** | One shared Docker stack (FastAPI + Postgres + LanceDB + Grafana) | n/a — it's code |
| **Commons** | Lives in shared services | Public namespace; everyone can read; publish requires user consent |
| **Personal toolkit** | Lives in shared services | `tenant_id` scoped — every query, every read, every write |

Why this beats Options Y / Z:

- **Option Y (per-user Docker stacks)** turns this into "we'll deploy your stack on Render for you" — fine, but loses the commons. There's no shared knowledge graph if every user is in their own isolated container. The "larger organizational thing" disappears.
- **Option Z (hybrid: shared platform + per-user data containers)** is operationally complex with no clear win over Option X for this use case. You only need it when individual tenant data volumes are too big for shared infra (we're nowhere near that).
- **Option X** is what GitHub, HuggingFace, Notion, Linear, Stripe, and basically every modern multi-tenant SaaS does. It's the proven pattern.

## Open work (the Pillar 0 items)

To execute Option X for hosted mode, the foundational work in [`ROADMAP.md`](/roadmap) Pillar 0:

- **Tenant abstraction** — `tenant_id` column on every personal-toolkit table, scoped queries everywhere, isolation tests
- **Auth interface** — `NoAuthBackend` (self-host) and `OAuthBackend` (hosted)
- **Config loader abstraction** — filesystem (self-host) ↔ multi-tenant config store (hosted)
- **`PLATFORM_MODE` env var** — single switch determines auth backend
- **Commons table partitioning** — public namespace for skills, agents, KG nodes that opted in to publish

Self-host stays exactly as it is today (Option X with `tenant_id="default"` — equivalently no multi-tenancy code path runs).

## What this means for the codebase

Already done:

- ✓ Single Docker stack (`platform/deploy/docker-compose.yml`) — works for self-host AND for "hosted = your own instance"
- ✓ render.yaml — deploys ONE instance per user trivially
- ✓ Web UI on Vercel (separate from agent backend) — this part IS shared across users; calls each user's own agent backend via `NEXT_PUBLIC_AGENT_URL`

Open work (Option X path, only if/when we add the shared sandbox):

- Tenant abstraction (`tenant_id` column on data, scoped queries)
- Auth interface
- Config loader abstraction
- `PLATFORM_MODE=multitenant` env var

Open work (Option Y path, the recommended one):

- A "deploy to Render" landing flow on humancensys.com that walks users through creating their instance
- Documentation explaining "your stack, your data, your bill"
- Optional: a thin routing layer that maps `<username>.humancensys.com` to the user's Render URL

## Two-mode discipline (the rule, regardless of topology)

Every change considers:

1. What does this look like for a self-host user?
2. What does this look like for a hosted-multitenant user (when that exists)?
3. Tests for both modes.
4. Docs for both modes.

## Anti-patterns to reject

- Hardcoded paths or queries without tenant scoping (in Option X path)
- "Multi-tenancy can come later" — once data is being written, retrofitting is a migration nightmare
- Mixing platform code and tenant data in the same git repo (tenant data NEVER lives in this repo)

## What this means concretely

**Self-host:** clone the repo, `docker compose up`, you have the engine running locally for you alone. `tenant_id="default"` is hardcoded in the no-auth path. Same code as hosted, just with auth disabled.

**Hosted (humancensys.com):** Liz operates one Docker stack as the engine. Users sign up, get tenant identities, share infrastructure but never share each other's personal toolkit data. Public skill library and public knowledge commons live in the shared services with a `public = true` marker.

This is the topology. The discipline that protects it: **every PR, every query, every write goes through the tenant abstraction.** Tests verify isolation. Anti-pattern catalog in [`/docs/contributing`](/docs/contributing).
